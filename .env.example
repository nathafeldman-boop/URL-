# Copie ce fichier vers .env puis renseigne ta clé (https://console.mistral.ai/api-keys)
MISTRAL_API_KEY=ta_cle_ici

# Optionnel — modèle utilisé pour l'audit (défaut : mistral-large-latest)
# MISTRAL_MODEL=mistral-small-latest

# Optionnel — port du serveur (défaut : 3000)
# PORT=3000
